import { FC } from 'react'
import styles from './Header.module.scss'

export const Header: FC = () => {
	return (
		<>
			<div className={styles.location}>ss</div>
			<header className={styles.header}>Header</header>
		</>
	)
}
